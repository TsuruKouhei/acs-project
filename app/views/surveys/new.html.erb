<div class="top-container container mt-5">
  <div class="iframe-container">
    <iframe src="https://app.chatsimple.ai/iframe23/de7c3014-4fb0-4271-a3c4-b41cc939a434/93482522-1995-47ce-b232-1e39da47e887/42bac7aa-c6d1-46f3-a05a-e01cd5f36c77" 
      height="600px" width="600px" title="Chatsimple">
    </iframe>
  </div>

  <div class="top-survey-form fadeUp">
    <h5>サービス向上のため<br>アンケートにご協力ください<i class="fa-solid fa-pen"></i></h5>
    
    <%= form_with(model: @survey, local: true, html: { class: 'needs-validation', novalidate: true }) do |form| %>
      <% if @survey.errors.any? %>
        <div id="error_explanation" class="alert alert-danger">
          <h2><%= pluralize(@survey.errors.count, "error") %>が検出されました。</h2>
          <ul>
            <% @survey.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="form-group">
        <%= form.label :title, 'チャットボットへの質問内容' %>
        <%= form.text_area :title, class: 'form-control' %>
      </div>

      <div class="form-group">
        <%= form.label :category_id, 'カテゴリー' %>
        <%= form.select :category_id, @categories, {}, class: 'form-control' %>
      </div>

      <div class="form-group">
        <%= form.label :crop_id, '作物' %>
        <%= form.select :crop_id, @crops, {}, class: 'form-control' %>
      </div>

      <div class="form-group form-check">
        <%= form.check_box :is_useful %>
        <%= form.label :is_useful, 'チャットボットの回答が役立った' %>
      </div>

      <div class="form-group form-check">
        <%= form.check_box :is_request %>
        <%= form.label :is_request, 'コンサルタントに対応を依頼する' %>
      </div>

      <div id="consultant_message">
        <p>今回のアンケート内容を<br>コンサルタントへ送信します。</p>
        <p>送信するボタンを押して返答を<br>お待ちください。</p>
      </div>

      <div class="actions text-center mt-3">
        <%= form.submit "送信する", class: 'btn btn-primary' %>
      </div>
    <% end %>
  </div>
</div>
